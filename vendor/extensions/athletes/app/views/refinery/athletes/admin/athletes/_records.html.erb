<%= render 'refinery/admin/search_header', :url => refinery.athletes_admin_athletes_path %>
<div class='pagination_container'>
  <% if @athletes.any? %>
    <%= render 'athletes' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
