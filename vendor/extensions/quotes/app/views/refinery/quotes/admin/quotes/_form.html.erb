<%= form_for [refinery, :quotes_admin, @quote] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @quote,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :name -%>
    <%= f.text_field :name, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:quote_text],
                :object => "quotes/quote" -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.quotes.admin.quotes.quote'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @quote.name) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
