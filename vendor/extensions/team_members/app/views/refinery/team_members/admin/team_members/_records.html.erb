<%= render 'refinery/admin/search_header', :url => refinery.team_members_admin_team_members_path %>
<div class='pagination_container'>
  <% if @team_members.any? %>
    <%= render 'team_members' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
